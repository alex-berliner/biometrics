SELECT
        h.time
        h.headache,
        m.medicine,
        s.sleep
FROM headache h
LEFT JOIN medicine m USING(time)
UNION ALL
SELECT
        h.time
        h.headache,
        m.medicine,
        s.sleep
FROM medicine m
LEFT JOIN headache h USING(time)
UNION ALL
SELECT
        h.time
        h.headache,
        m.medicine,
        s.sleep
FROM sleep s
LEFT JOIN headache h USING(time)
order by h.time;
